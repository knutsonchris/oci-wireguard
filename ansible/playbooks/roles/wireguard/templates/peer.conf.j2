[Interface]
PrivateKey = {{ (peer_private_keys.results | selectattr('item.name', 'equalto', name) | list)[0].stdout }}
Address = {{ allowed_ips }}
DNS = 1.1.1.1

[Peer]
PublicKey = {{ server_public_key.stdout }}
AllowedIPs = 0.0.0.0/0
Endpoint = {{ inventory_hostname }}:{{ wireguard_listen_port }}
